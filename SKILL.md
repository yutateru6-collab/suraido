---
name: suraido
description: 高校生向け英文法解説スライドを、Google Drive上の共通背景素材・固定タイポグラフィ・授業向けレイアウト規則に従って作成・修正するスキル。背景はDriveのsuraido/backgroundsから再利用し、bg01を基準にしつつ毎回同一背景の使い回しは避ける。大きく太い文字、十分な余白、そして「1クリック1要素」の段階表示を必須条件とする。
---

# suraido — 英文法解説スライド標準スキル

高校生向けの英文法解説スライドを作るときの正典。Google Drive / Google Slides / PowerPointで英文法教材を作成・修正するときは、最初にこのファイルと `references/` を読む。

## 最重要ルール

1. **背景を毎回ゼロから作らない。** まず `references/backgrounds.md` にあるGoogle Drive素材庫を参照する。
2. **bg01_standard_stationery.png をデザインの基準にする。** 白〜薄クリームの大きな余白、パステル青・黄・コーラル、端にだけ学習モチーフ、中央は読みやすく空ける。
3. **全部同じ背景にはしない。** 同じデッキでは2〜4種類を使い分ける。単元に合う背景を選び、同一画像の連続使用は原則2枚まで。
4. **見た目より授業での視認性を優先する。** 高校教室の後方から読めるサイズを基準にする。
5. **文字は大きく、太く、短く。** 小さい文字で情報を詰め込まない。
6. **1スライド=1つの文法ポイント。** 例文・注意点・比較を詰め込みすぎない。
7. **英文は不自然な位置で改行しない。** 句・節の切れ目を優先し、単語1個だけ次行に残す孤立折返しを避ける。
8. **「1クリック1要素」の段階表示は毎回必須。例外扱いしない。** タイトルは最初から表示してよいが、核心ルール・形/公式・サイン語・例文・注意点・まとめ等の主要要素は、クリックするたびに1つずつ出す。最初のクリックで次ページへ進む状態は失敗。ユーザーが明示的に「アニメーションなし」と指定した場合のみ解除できる。
9. **Google Slides依頼でもクリック表示要件を落とさない。** Slides API等で確実な段階表示を設定できない経路なら、PPTXでアニメーションを設定・検証してからDriveへ保存する。できない場合は「できた」と偽らない。
10. **完成後は全ページ目視＋クリック順を確認。** 文字切れ、折返し、余白、コントラスト、背景と文字の干渉に加え、各スライドでクリックが要素表示に使われ、表示が尽きるまで次ページへ飛ばないことを確認する。
11. **ユーザーの明示指示が最優先。** このスキルは標準値であり、個別指定があれば上書きする。ただし「1クリック1要素」は、ユーザーが明示的に解除しない限り必須。

## 作業フロー

1. `references/google-drive-workflow.md` を読み、Driveの背景素材庫を確認する。
2. `references/backgrounds.md` から単元に合う背景を選ぶ。原則bg01を軸にする。
3. `references/style-guide.md` のフォント・文字サイズ・色・余白ルールで組む。
4. 内容を「見出し → 核心ルール → 形 → 例文 → 注意点」の順で整理する。
5. **必ず1クリック1要素の段階表示を設定する。** `On Click` を基本とし、主要要素を別々のクリックに割り当てる。単なるページ送りだけの状態では納品しない。
6. **実ファイルでクリック順を検証する。** 1クリック目から要素が出ること、主要要素が1個ずつ出ること、最後の要素の後にだけ次ページへ進めることを確認する。
7. 全ページをレンダリング／サムネイルで確認し、必要なら1回以上修正する。
8. 新しい良質な背景を作った場合はDrive素材庫へ追加し、`references/backgrounds.md` に追記する。

## 背景の選び方

- 標準解説: bg01 / bg02 / bg10
- 会話・話法・例文対比: bg03
- 使い分け・判断フロー: bg04
- 時制・完了・時間関係: bg05
- 「見分ける」「違いを発見」: bg06
- 板書感・まとめ: bg07
- チェック項目・試験対策: bg08
- 複数ポイント整理: bg09

詳細は `references/backgrounds.md` を参照。
